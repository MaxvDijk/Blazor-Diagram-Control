@page "/"
@using BlazorDiagramControl.Components.Behavior
@using System.Text.Json.Serialization
@using System.Reflection
@rendermode InteractiveServer

<DiagramComponent DiagramBehavior=@maxBehavior/> @* todo: nameOF*@


    @code{

    //private string teststring = Assembly.CreateQualifiedName(Assembly.GetCallingAssembly().GetName().ToString(), "MaxBehavior");
     //   [JsonIgnore]
        private DiagramBehavior maxBehavior = new MaxBehavior();
        
}