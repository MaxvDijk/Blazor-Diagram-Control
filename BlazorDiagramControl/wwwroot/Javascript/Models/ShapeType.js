class ShapeType {
    constructor(
        tag,
        color,
        width,
        height
    ) {
        this.tag = tag;
        this.color = color;
        this.width = width;
        this.height = height;
    }
}